using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Control_Button_NewGame : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        
    }

    public void CreateNewGameSession() {
        // TODO: Prompt for name of game session
        // TODO: Transition to the main game scene
        // ?: Set the game session name to PlayerPrefs so that it can be easily loaded
    }
}
