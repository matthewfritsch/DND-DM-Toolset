using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Control_Button_LoadGame : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        
    }

    public void LoadGameFromList() {
        // TODO: Open List of all available saved games
        // ?: Scan the folder and place all the entries into a scrollable list to click and select from
        // 
    }
}
